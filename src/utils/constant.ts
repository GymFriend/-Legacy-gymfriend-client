export const clientPath: string = `http://${window.location.hostname}:3000`;
export const serverPath: string = `http://${window.location.hostname}:8080`;
export const dateFormatYMD: string = "yyyy-MM-dd";
export const joinableChallenge: number[] = [1, 3, 5, 7];
console.log(clientPath);
console.log(serverPath);
